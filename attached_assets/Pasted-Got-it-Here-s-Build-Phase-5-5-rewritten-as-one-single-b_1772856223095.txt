Got it. Here’s Build Phase 5.5 rewritten as one single block prompt:

Build Phase 5.5 for Franchise OS / Xpansion Console as a Growth Engine that helps franchise owners identify revenue opportunities, execute actions, and improve weekly outcomes. Implement: (1) Opportunity Pipeline + Actions Queue with auto-generated opportunities from declining trends, threshold misses, and underperforming scorecards, plus assignable actions with statuses open/in_progress/blocked/done; endpoints: GET /api/tenants/:tenantId/opportunities, POST /api/tenants/:tenantId/opportunities/:id/actions, GET /api/tenants/:tenantId/actions?status=&owner=&locationId=, PUT /api/tenants/:tenantId/actions/:id; UI: /actions page with opportunity cards (impact estimate) and kanban/list toggle. (2) Campaign Attribution + Performance Tracking with campaign tracking (promo/local outreach/staffing initiative/upsell push), attribution of outcome deltas to campaign windows, and pre/post performance comparison; endpoints: GET /api/tenants/:tenantId/campaigns, POST /api/tenants/:tenantId/campaigns, PUT /api/tenants/:tenantId/campaigns/:id, GET /api/tenants/:tenantId/campaigns/:id/impact; UI: /campaigns page with timeline and impact summary (absolute change, % change, confidence level). (3) Weekly Executive Digest + Coaching Summary that generates weekly tenant digest including wins, risks, unresolved alerts, and top 5 recommended actions, plus coach-voice narrative summary; endpoints: POST /api/admin/digests/:tenantId/run, GET /api/admin/digests/:tenantId/history; UI: /admin/digests page with run-now, history, and summary viewer. (4) Location Benchmarking + Playbooks with ranking by composite score/trend momentum/risk, detection of top-performer patterns, and repeatable playbook application to underperforming locations; endpoints: GET /api/tenants/:tenantId/benchmarking, GET /api/tenants/:tenantId/playbooks, POST /api/tenants/:tenantId/playbooks, POST /api/tenants/:tenantId/playbooks/:id/apply; UI: /benchmarking and /playbooks pages. (5) Goals + Accountability Workflow with tenant/location goals by metric/period, automatic on-track/off-track status, and weekly check-ins with owner/manager notes; endpoints: GET /api/tenants/:tenantId/goals, POST /api/tenants/:tenantId/goals, PUT /api/tenants/:tenantId/goals/:id, POST /api/tenants/:tenantId/goals/:id/checkins; UI: /goals page with goal scorecards and check-in timeline. (6) API hardening + instrumentation requirements: standard JSON responses success {ok:true,data:...} and error {ok:false,error:{code,message,details}}, auth + tenant scoping on all new endpoints, Zod validation on all write payloads, audit log all mutations, analytics events for action creation/completion, campaign creation, digest generation, and goal check-ins. Acceptance criteria: opportunities generated per tenant, actions create/assign/update/complete, campaign impact returns meaningful deltas, weekly digests generate and persist in history, benchmarking ranks correctly, goals/check-ins persist with accurate status, all endpoints use standardized JSON, tenant isolation verified. On completion output: files changed, SQL migrations, new tables/indexes, endpoints added (method+path), validation schemas, UI routes/pages changed, acceptance checklist with pass/fail evidence, and known gaps/TODOs.