Build Phase 5.6 — Enterprise Reliability + Intelligence Loop for Franchise OS / Xpansion Console with the objective of making the platform enterprise-secure, operationally resilient, and outcome-learning; implement scope in six workstreams: (1) Security Defense-in-Depth: add PostgreSQL RLS policies for all tenant-owned tables (enforced by tenant_id + role context), require MFA for owner/superadmin users, add fine-grained permission checks (resource + action level), implement break-glass superadmin access flow with reason capture + expiry + alerting + audit trail, and add immutable audit event hashing (store event_hash + prev_hash chain); (2) Durable Job Orchestration: replace setInterval critical tasks with a queue-backed job system (digest generation, risk recompute, campaign impact recalcs, scheduled reports, notifications retry), require idempotency keys on all enqueued jobs, add exponential backoff retries, dead-letter queue, dedupe guard, per-tenant concurrency limit, and job lock timeout handling; (3) Data Confidence Engine: create confidence scoring for metric streams by freshness, completeness, continuity, outlier rate, and sample sufficiency, persist confidence snapshots per tenant/location/metric/period, and make risk score, opportunity ranking, and campaign impact confidence-aware (confidence_weighted outputs); (4) Recommendation Outcome Learning: track recommendation lifecycle (generated, viewed, accepted, rejected, converted_to_action, completed), link outcomes to metric deltas over defined windows, compute recommendation effectiveness scores, and auto-rank recommendation/playbook templates by measured uplift and confidence; (5) Execution UX Upgrade: add /inbox Command Inbox page aggregating top risks, blocked/overdue actions, off-track goals, unresolved critical alerts, and highest-confidence opportunities with one-click conversion to actions, plus “Why this?” explainability drawer showing factors, weights, confidence, and last refresh timestamp; (6) API + Ops Robustness: add /api/v1 namespace for all new endpoints, publish OpenAPI spec for new modules, add contract tests for success/error schema shape, enforce standard response envelopes ({ok:true,data} / {ok:false,error:{code,message,details}}), add structured logs + tracing IDs on all write/mutation paths, and expose ops metrics (queue depth, job success rate, retry count, p95 latency, tenant error rates) in Ops Health; required DB additions include tables (or equivalent): job_queue, job_runs, job_dead_letters, tenant_confidence_snapshots, recommendation_events, recommendation_effectiveness, security_access_events, break_glass_sessions, and audit hash columns/indexes; required endpoints include: POST /api/v1/admin/jobs/:type/enqueue, GET /api/v1/admin/jobs/runs, GET /api/v1/tenants/:tenantId/confidence, GET /api/v1/tenants/:tenantId/inbox, GET /api/v1/tenants/:tenantId/recommendations/effectiveness, POST /api/v1/superadmin/break-glass/start, POST /api/v1/superadmin/break-glass/end; acceptance criteria (must pass): RLS blocks cross-tenant reads/writes in integration tests, MFA enforced for owner/superadmin login, break-glass sessions require reason and emit alerts/audit entries, queued jobs are idempotent and recover after worker restart without duplication, dead-letter captures terminal failures, confidence scores appear in API and UI and influence ranking, recommendation effectiveness updates after action completion windows, /inbox loads prioritized actionable items with explainability, OpenAPI docs generated for all Phase 5.6 endpoints, and all new endpoints return standardized response shape with tenant isolation verified; when complete output: files cha